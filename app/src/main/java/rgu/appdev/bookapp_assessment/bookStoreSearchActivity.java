package rgu.appdev.bookapp_assessment;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class bookStoreSearchActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_book_store_search);
    }

    //TODO build bookstore search functionality
    //Already got the API key ready to go - saved in strings
}